# Changelog

## [Unreleased]

## [1.0.5] - 2026-09-23 (Preview)

### Changed

- 群聊以最近设置条数内所有人的消息及发言人理解当前话题；指定回复对象时优先接该对象的可见发言。用户自己的 6–12 条短消息继续只用作可选口吻样本。
- 起草、JEV 预判与复审明确区分引用图片作者、引用者和用户；图片正文不可见时不擅自确认规则变化或编造自己的经历。

### Fixed

- OCR 对引用图片保留原作者及正文未完整识别的标记，避免把群友截图误当作用户所发。
- 按前后画面的消息顺序去重，避免短句只差一个字或重复短句时漏掉其间的新群消息。

### Known issues

- Windows“填入微信”在窗口布局变化时偶发切到其他会话，尚未修复。本预览版请关闭自动发送，核对当前会话后再填入；不确定时改用复制。
- macOS 仍为未签名、未公证预览版，真实 Mac 微信读取与填入尚待实机验收，自动发送禁用。

## [1.0.4] - 2026-09-23 (Preview)

### Added

- 新增 Apple Silicon macOS 预览构建与本地微信窗口采集、手动填入适配；Mac 自动发送保持禁用，真实微信全流程仍待实机验收。
- 发布流水线分别在 Windows 和 macOS runner 构建，通过自动测试与 Mac 启动检查后才发布双平台包。

### Fixed

- 修复 Windows 打包时混入第三方 ICU DLL 导致 QtCore 启动失败。
- 群聊可明确指定回复对象；TypeSafe 返回 403 / Cloudflare 1010 时提示访问被拒并停止重复请求。

### Documentation

- 项目介绍明确 Windows 正式版与 Mac 未签名、未公证预览版的区别和使用边界。

## [1.0.3] - 2026-09-22

### Changed

- TypeSafe JEV 判断卡直接展示官方返回的意图、把握度、选项概率、风险分数和高风险概率，不再用本地阈值伪造二元结论。
- 聊天风格模仿改为当前运行期内同一会话至少 6 条、最多最近 12 条我方有效短消息；与「参考上下文」的对话理解条数分开计算。
- README、项目说明和版本记录改为本仓库真实功能与 `v1.0.x` 历史，不再沿用上游项目的产品文案和 `v0.1.x` 更新记录。
- 新增 `NOTICE.md`，明确上游来源、本仓库修改范围以及非官方发行关系。

## [1.0.2] - 2026-09-22

### Added

- JEV 判断卡片增加危险等级与安全提示、真实意图把握度、对方需要、建议行动和是否适合给出实质内容。
- JEV 判断紧张已缓解时在卡片内明确提示。

### Fixed

- 保留并校验 TypeSafe JEV 返回的选择题置信度，避免界面丢失“把握度”。

## [1.0.1] - 2026-09-22

### Changed

- TypeSafe JEV 现在先判断意图、风险和行动，再把结构化结果交给 DeepSeek 生成候选。
- 候选生成后由 TypeSafe JEV 复审质量并排序；全部不合格时最多重写一次。
- 第二次复审仍未通过时保留候选供人工确认，但强制禁止自动发送。
- DeepSeek 起草温度从 1.2 降为 0.6，减少跑偏和不稳定话术。
- 主界面底部增加可点击的 GitHub 图标，并显示与发布标签一致的客户端版本号。

### Fixed

- 更新检查优先通过 GitHub Releases 发布页识别版本，避免匿名 API 限流导致漏掉更新提示。
- 发布流水线在打包前校验客户端版本号与 Git 标签完全一致。

## [1.0.0] - 2026-09-22

### Added

- Windows 微信旁挂回复助手，使用本地 OCR 读取当前聊天。
- DeepSeek 官方接口生成三条候选话术。
- TypeSafe 官方 JEV 直连判断意图、风险并按真实概率排序候选。
- 主界面“3秒自动发送”开关，带可取消倒计时、会话变化和微信前台状态门禁。
- 关系背景、说话风格、上下文条数和群聊回复对象设置。

### Security

- DeepSeek 与 TypeSafe API Key 只保存在 Windows 当前用户环境变量，不写入项目配置或日志。
- 自动发送默认关闭，只有用户显式开启后才会生效。
